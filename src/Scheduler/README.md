# Scheduler

Blazor component library providing month, week and day scheduler views with draggable and resizable events.

```razor
<SchedulerComponent CurrentDate="DateTime.Today"
                    View="SchedulerView.Week"
                    Events="events"
                    OnEventChanged="OnEventChanged" />
```

Package content includes static assets at `_content/Scheduler`.
